# Netlify Configuration for Tensora Digital Solutions
[build]
  base = "WEBSITE FRONTEND"
  publish = "dist"
  command = "npm run build"

# SPA Fallback rule for React Router (Prevents 404 on refresh)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
